<section
  id="core"
  class="relative overflow-hidden border-y border-slate-800 bg-[#070709] px-6 py-24"
>
  <!-- Luces decorativas del fondo -->
  <div
    class="absolute -left-32 top-1/2 h-80 w-80 -translate-y-1/2 rounded-full bg-blue-600/20 blur-[120px]"
  ></div>

  <div
    class="absolute -right-32 top-1/2 h-80 w-80 -translate-y-1/2 rounded-full bg-purple-600/20 blur-[120px]"
  ></div>

  <!-- Contenedor principal -->
  <div
    class="relative z-10 mx-auto grid max-w-6xl items-center gap-12 lg:grid-cols-2"
  >
    <!-- Imagen de Core -->
    <div class="order-2 lg:order-1">
      <div
      id="core-card"
        class="overflow-hidden rounded-3xl border border-blue-500/20 bg-slate-900/40 p-2 shadow-2xl shadow-blue-950/30 hover:border-purple-400/60 hover:shadow-purple-500/40"
      >
        <img
          src="/core-hesed.png"
          alt="Core TrimmOS"
          class="h-auto w-full rounded-2xl object-cover float-core"
          loading="lazy"
        />
      </div>
    </div>

    <!-- Texto de la sección -->
    <div class="order-1 text-center lg:order-2 lg:text-left">
      <p
        class="mb-4 text-sm font-semibold uppercase tracking-[0.35em] text-blue-400"
      >
        Parte de la familia TrimmOS
      </p>

      <h2
        class="mb-6 text-4xl font-extrabold italic tracking-tight text-white md:text-6xl"
      >
        Conoce a
        <span
          class="bg-gradient-to-r from-blue-400 to-purple-500 bg-clip-text text-transparent"
        >
          Core
        </span>
      </h2>

      <p class="mb-5 text-lg leading-8 text-slate-300">
        Core es la mascota oficial de TrimmOS y representa la energía, la innovación y el crecimiento que queremos impulsar en cada barbería y salón.
      </p>

      <p class="mb-8 leading-7 text-slate-400">
        Forma parte de nuestra identidad y estará presente en la comunidad, las novedades y cada nueva etapa de TrimmOS.
      </p>

      <!-- Características -->
      <div class="mb-8 grid gap-4 sm:grid-cols-2">
        <div
          class="rounded-2xl border border-slate-800 bg-white/[0.03] p-4"
        >
          <h3 class="mb-1 font-bold text-white">Identidad TrimmOS</h3>
          <p class="text-sm text-slate-400">
            Representa el estilo y la esencia de la plataforma.
          </p>
        </div>

        <div
          class="rounded-2xl border border-slate-800 bg-white/[0.03] p-4"
        >
          <h3 class="mb-1 font-bold text-white">Innovación</h3>
          <p class="text-sm text-slate-400">
            Simboliza nuestra búsqueda constante de nuevas soluciones.
          </p>
        </div>

        <div
          class="rounded-2xl border border-slate-800 bg-white/[0.03] p-4"
        >
          <h3 class="mb-1 font-bold text-white">Comunidad</h3>
          <p class="text-sm text-slate-400">
            Acompaña a barberías y salones que crecen con TrimmOS.
          </p>
        </div>

        <div
          class="rounded-2xl border border-slate-800 bg-white/[0.03] p-4"
        >
          <h3 class="mb-1 font-bold text-white">Crecimiento</h3>
          <p class="text-sm text-slate-400">
            Representa el impulso de cada negocio hacia su mejor versión.
          </p>
        </div>
      </div>
    </div>
  </div>
  </div>

<!-- Firma de Core -->
<div class="mt-10 border-t border-slate-800 pt-8 text-center">
  <p class="text-lg text-slate-300 leading-relaxed max-w-xl mx-auto">
    <span class="font-semibold bg-gradient-to-r from-blue-400 to-purple-500 bg-clip-text text-transparent">
      💜 Core
    </span>
    forma parte de la familia
    <span class="font-semibold text-white">
      TrimmOS.
    </span>
  </p>

  <p class="mt-4 text-sm text-slate-400 leading-relaxed max-w-lg mx-auto">
    Lo verás crecer junto con nuestra comunidad, acompañando cada nueva función,
    cada actualización y cada paso que demos hacia el futuro.
  </p>

  <p class="mt-6 text-xs uppercase tracking-[0.35em] text-slate-500">
    Core™ · Mascota Oficial de TrimmOS
  </p>
</div>

</div>
</section>
<script>
  const card = document.getElementById("core-card");

  if (card) {
    card.style.transformStyle = "preserve-3d";
    card.style.willChange = "transform";

    let animationFrame = 0;

    card.addEventListener("mousemove", (event) => {
      const rect = card.getBoundingClientRect();

      const mouseX = event.clientX - rect.left;
      const mouseY = event.clientY - rect.top;

      const centerX = rect.width / 2;
      const centerY = rect.height / 2;

      const rotateY = ((mouseX - centerX) / centerX) * 8;
      const rotateX = -((mouseY - centerY) / centerY) * 8;

      cancelAnimationFrame(animationFrame);

      animationFrame = requestAnimationFrame(() => {
        card.style.transition = "none";

        card.style.transform = `
          perspective(900px)
          rotateX(${rotateX}deg)
          rotateY(${rotateY}deg)
          scale(1.02)
        `;
      });
    });

    card.addEventListener("mouseleave", () => {
      cancelAnimationFrame(animationFrame);

      card.style.transition = "transform 500ms ease";

      card.style.transform =
        "perspective(900px) rotateX(0deg) rotateY(0deg) scale(1)";
    });
  }
</script>